<Window x:Class="Library_C__Group_Project.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library_C__Group_Project"
        mc:Ignorable="d"
        Title="BookWindow" Height="500" Width="800">
    <Grid>
        <Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">

                <TextBlock Text="Title"/>
                <TextBox Name="TitleTextBox"/>

                <TextBlock Text="Author"/>
                <TextBox Name="AuthorTextBox"/>

                <TextBlock Text="ISBN"/>
                <TextBox Name="ISBNTextBox"/>

            </StackPanel>

            <Button Grid.Row="1"
            Name="AddBookButton"
            Content="Add Book"
            Height="30"
            Margin="400,10,50,10" Click="AddBookButton_Click"/>
            
            
            <Button Grid.Row="1"
            Name="RemoveBookButton"
            Content="Remove Selected Book"
            Height="30"
            Margin="50,10,400,10"
            Click="RemoveBookButton_Click"/>

            <StackPanel Grid.Row="2">
                <StackPanel   Orientation="Horizontal"
            Margin="0,0,0,10">

                <TextBox Name="SearchTextBox"
             Width="200"
             Height="20"
             Margin="0,0,10,0"/>

                <Button Name="SearchButton"
            Content="Search"
            Width="70"
            Click="SearchButton_Click"/>

                <Button Name="ShowAllButton"
            Content="Show All"
            Width="70"
            Margin="10,0,0,0"
            Click="ShowAllButton_Click"/>
            
            </StackPanel>
            <ListBox
            Name="BooksListBox"
            Height="125" SelectionChanged="BooksListBox_SelectionChanged"/>
            </StackPanel>
            <StackPanel Grid.Row="4">
                <TextBlock Text="Reservation Queue"
        FontWeight="Bold"
            Margin="0,0,0,0"/>
                <ListBox Name="ReservationQueueListBox"
                Height="80"/>
                
            </StackPanel>


        </Grid>
    </Grid>
</Window>
